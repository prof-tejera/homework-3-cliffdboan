import { useState } from "react";
import "./Pager.css";

const Pager = () => {
  const [index, totalPages] = useState(null);

  // hilight current page
  // left button indexes down one
  // right button indexes up one
  // selecting a specific number changes page to selected num
  // have the page range from 1 to a var named totalPages
  // // Have an input form that updates totalPages

  // return (
  // );
};

export default Pager;
